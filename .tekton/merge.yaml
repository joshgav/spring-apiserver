apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: spring-apiserver-default-pipeline
  namespace: spring-apiserver-tekton
  annotations:
    pipelinesascode.tekton.dev/on-target-branch: "[main,tekton]"
    pipelinesascode.tekton.dev/on-event: "[push,pull_request]"
    pipelinesascode.tekton.dev/task: "git-clone"
    pipelinesascode.tekton.dev/max-keep-runs: "5"
  labels:
    tekton.dev/pipeline: spring-boot-pipeline
    type: pipeline
spec:
  params:
    - name: source-repo
      value: 'https://github.com/joshgav/spring-apiserver.git'
    - name: source-branch
      value: tekton
    - name: output-image
      value: registry-quay-quay.apps.ipi.aws.joshgav.com/joshgav/spring-apiserver
  pipelineRef:
    name: spring-boot-pipeline
  serviceAccountName: pipeline
  workspaces:
    - name: source-folder
      volumeClaimTemplate:
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 1Gi